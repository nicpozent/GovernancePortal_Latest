# Parses the pino JSON the API/DB write to stdout. Timestamps are ISO
# (pino.stdTimeFunctions.isoTime), e.g. 2026-06-30T11:30:12.345Z.
[PARSER]
    Name        pino_json
    Format      json
    Time_Key    time
    Time_Format %Y-%m-%dT%H:%M:%S.%LZ
    Time_Keep   On
