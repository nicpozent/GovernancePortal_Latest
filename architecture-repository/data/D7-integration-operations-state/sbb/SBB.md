# SBB — integration_config (singleton), sync_runs

_Solution Building Block realizing ABB **D7 Integration & Operations State** in the Birgma Governance Portal._

## Realization
Single-row config; secrets never returned in full (only "set?" booleans).

## Where it lives (code)
`apps/api/db/migration_017_integrations.sql`

## Maturity
Production-grade
