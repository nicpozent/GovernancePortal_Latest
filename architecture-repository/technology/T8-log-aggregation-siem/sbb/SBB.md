# SBB — stdout JSON → external SIEM (via feed/forward)

_Solution Building Block realizing ABB **T8 Log Aggregation / SIEM** in the Birgma Governance Portal._

## Realization
Structured logs + audit feed/forward exist; aggregation + alerting is an org deployment (gap).

## Where it lives (code)
`apps/api/src/logger.js`

## Maturity
Optional (aggregation not deployed)
