# SBB — nginx (alpine)

_Solution Building Block realizing ABB **T2 HTTP Edge / TLS Termination** in the Birgma Governance Portal._

## Realization
TLS 1.2/1.3; serves SPA; proxies /api; security headers/CSP.

## Where it lives (code)
`apps/web/nginx.conf, apps/web/Dockerfile`

## Maturity
Production-grade
