# SBB — services/sharepoint.js (Graph drive items)

_Solution Building Block realizing ABB **A6 Content Access Broker** in the Birgma Governance Portal._

## Realization
Per-site Sites.Selected grant; returns webUrl + short-lived download URL; version label is source of truth.

## Where it lives (code)
`apps/api/src/services/sharepoint.js`

## Maturity
Production-grade
