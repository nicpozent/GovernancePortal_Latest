# SBB — React 19 + MSAL.js (Vite 8 build)

_Solution Building Block realizing ABB **A4 Presentation / Experience** in the Birgma Governance Portal._

## Realization
PKCE; sessionStorage token cache; 15-min idle logout; runtime config injected at container start.

## Where it lives (code)
`apps/web/src/*, 40-envconfig.sh`

## Maturity
Production-grade
